pipeline {
    agent any

        stages {
            stage('Clone repository') {
                    steps {
                        script{
                            checkout scm
                        }
                }
        }
    }
}
